/**
 * Stub for jsPDF optional dependencies (canvg, html2canvas, dompurify).
 * These are only needed for SVG/HTML-to-PDF rendering, which we don't use.
 * This prevents Vite's import analysis from erroring in dev mode.
 */
export default {};

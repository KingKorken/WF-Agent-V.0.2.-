{
  "name": "@workflow-agent/local-agent",
  "version": "0.1.0",
  "description": "Electron desktop agent — receives commands from cloud, executes on user's machine",
  "main": "dist/src/main.js",
  "scripts": {
    "build": "npm run build --workspace=@workflow-agent/shared && tsc",
    "dev": "npm run build --workspace=@workflow-agent/shared && tsc && electron dist/src/main.js",
    "test-server": "npm run build --workspace=@workflow-agent/shared && tsc && node dist/test/test-server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@workflow-agent/shared": "*",
    "dotenv": "^16.4.0",
    "playwright": "^1.40.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  }
}
